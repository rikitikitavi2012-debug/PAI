Прямо отвечу на твой вопрос.

## Честная оценка

**Algorithm v4.0-alpha — 11/14 Autoresearch механизмов проверены.** Непроверенные 3:

| Механизм | Почему не покрыт | Как покрыть |
|----------|-----------------|-------------|
| Slow gates | Ни одна задача не дошла до 5+ итераций с тяжёлым тестом | Нужна задача где `bun test` = slow gate |
| Context recovery | Требует /compact mid-loop | Ты вручную жмёшь /compact на итерации 3-4 |
| Multiple [Q] | Обе задачи имели 1 [Q] | Нужна задача с 2+ числовых метрик |

**Но Algorithm — это не только Autoresearch.** Есть механизмы основного цикла которые тоже нужно верифицировать:

| Механизм | Статус |
|----------|--------|
| ISC Count Gate (отказ при < floor) | ❌ Не тестировал отказ |
| ISC escape hatch (downshift to NATIVE) | ❌ Не тестировал |
| Capability invocation check (VERIFY) | ✅ Частично |
| Context compaction at phase transitions | ❌ Не было длинных сессий |
| PRD continuation после перезапуска | ❌ Не тестировал |

**По обучению — честно:**
- LEARN.md пишу, Wisdom Frames обновляются автоматически (hooks)
- Но LEARN содержимое поверхностное — "что работает/не работает", мало **actionable insights**
- Не проверял, читает ли следующая сессия эти learnings и применяет

## Предлагаю одну задачу, покрывающую ВСЁ

**Задача: оптимизация PAI hooks — 2 метрики + slow gates + context recovery.**

[Q]-1: Средний размер hook файлов (кБ) — уменьшить дублирование кода между hooks
[Q]-2: Количество passing тестов — увеличить покрытие (сейчас 261 pass, 7 fail)

Это даёт:
- **Multiple [Q]**: 2 метрики, sequential optimization
- **Slow gates**: `bun test` = slow gate (5+ секунд), каждые 5 итераций
- **Context recovery**: ты жмёшь `/compact` на итерации 3-4, я восстанавливаюсь из PRD + experiments.tsv
- **10+ итераций**: рефакторинг hooks — много маленьких changes
- **ISC Count Gate**: Deep tier = 40+ ISC minimum
- **Real value**: система реально улучшается, не synthetic test

Запускать?
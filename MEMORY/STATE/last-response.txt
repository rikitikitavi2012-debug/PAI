═══ PAI | NATIVE MODE ═══════════════════════
🗒️ TASK: Объяснение сбоя и текущий статус
📃 CONTENT:

**Что произошло:** Агент-Engineer (PR #931) получил задачу с `isolation: worktree`, но внутри сделал `git checkout main` в **основном** репозитории вместо своего worktree. Ветка `main` = community PAI без наших файлов → все 30 hooks, MEMORY, settings исчезли.

**Почему:** Агент не понял границы worktree. Он пытался синхронизировать fork с upstream и выполнил checkout в корневой директории. Sandbox заблокировал ему push/PR, но checkout уже прошёл.

**Последствия:** 
- Stop hooks не нашлись → Claude Code показал 5 ошибок
- Statusline пропал (зависит от hooks)
- Ничего не потерялось — `git checkout master` вернул всё за 2 секунды

**Сейчас всё ОК:**
- master branch, 30 hooks на месте
- PR #934 создан вручную
- Stash восстановлен

**Урок в MEMORY:** Агентам нельзя давать задачи с `git checkout` в основном репо. Worktree isolation не защищает от этого — агент видит и основной репо тоже.

🗣️ Navi: Агент сменил ветку в основном репо — сбой на минуту. Всё восстановлено, данные не потеряны. Система работает штатно.
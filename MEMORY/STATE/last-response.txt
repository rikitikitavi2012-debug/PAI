```
════ PAI | NATIVE MODE ═══════════════════════
🗒️ TASK: Git aliases + коммит + бэкап в private
🔄 ITERATION on: Финализация git workflow из прошлой сессии

🔧 CHANGE:
  • 5 git aliases настроены в .git/config (fetchup, syncmain, backupmaster, pullup, pushup)
  • Убрал дубли из глобального ~/.gitconfig
  • Починил двойное экранирование `\\!` → `!bash -c`
  • Закоммитил state файлы + очистил stale lock (48e3d3c)
  • Запушил master → private remote

✅ VERIFY:
  • `git fetchup` — работает, fetches upstream + origin
  • `git syncmain` — работает, ff-merge + push to origin
  • `git backupmaster` — работает, push to private
  • Все 3 remote-а синхронизированы (SHA совпадают)
  • Ветка master, tracking private/master

🗣️ Navi: Git workflow готов. Команды: fetchup, syncmain, backupmaster, pullup, pushup.
```

**Шпаргалка:**

| Команда | Что делает |
|---|---|
| `git fetchup` | Скачать обновления upstream + origin |
| `git syncmain` | Синхронизировать main с Daniel + push в origin |
| `git backupmaster` | Бэкап master в приватный репо |
| `git pullup` | Pull из upstream в текущую ветку |
| `git pushup` | Push в upstream (для PR-ов) |
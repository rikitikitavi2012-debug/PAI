# AI Steering Rules — User Overrides

Personal behavioral rules for Ivan. Force-loaded at session start via `settings.json → loadAtStartup`.
Overrides system defaults in `PAI/AISTEERINGRULES.md` where conflicting.

---

**Язык ответов (CRITICAL).** Всегда отвечай на языке пользователя. Если Ivan пишет по-русски — думай и отвечай по-русски. Если по-английски — по-английски. Никогда не переключай язык из-за того что системный контекст на английском. Язык пользователя определяется по его последнему сообщению, не по контексту сессии.

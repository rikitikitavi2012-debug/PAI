# User Custom Pipelines

Place user-customized pipeline definitions here. Pipelines chain multiple actions into automated workflows.

Pipelines follow the pattern: `<name>.pipeline.ts`

Example: `morning-report.pipeline.ts`
